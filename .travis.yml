language: shell
os:
  - linux
  - osx
  - windows
env:
  - STACK_ROOT=$HOME/.stack
cache:
  directories:
    - $STACK_ROOT
    - .stack-work
install:
  - curl --location https://get.haskellstack.org | sh
script:
  - stack build --haddock --test
  - stack sdist
